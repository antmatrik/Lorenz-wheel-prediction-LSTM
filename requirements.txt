# Union of every model's dependencies -- convenient for local development and the test
# suite (which exercises both the TensorFlow LSTM and the PyTorch TCN). pip de-duplicates
# the shared packages (numpy, matplotlib).
#
# Per-model / per-notebook subsets live under requirements/:
#   requirements/lstm.txt  -> LSTM training  (numpy, tensorflow, matplotlib)
#   requirements/tcn.txt   -> TCN training   (numpy, torch, matplotlib)
#   requirements/eval.txt  -> evaluation     (numpy, tensorflow, torch)
-r requirements/lstm.txt
-r requirements/tcn.txt
