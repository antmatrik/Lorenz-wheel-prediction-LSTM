# Direct dependencies only. A full `pip freeze` would also list TensorFlow's
# transitive deps (absl-py, grpcio, h5py, protobuf, ml_dtypes, keras, ...);
# those are intentionally omitted and left for pip to resolve.
#
# Versions are unpinned on purpose: on Google Colab -- where numpy, TensorFlow
# and matplotlib are preinstalled and integrated with the CUDA/GPU stack --
# `pip install -r requirements.txt` becomes effectively a no-op and won't
# disturb the preinstalled environment. Add pins if you need a reproducible
# local build.
numpy
tensorflow
matplotlib
