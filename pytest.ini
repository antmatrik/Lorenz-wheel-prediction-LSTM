[pytest]
# Tests import the scripts under src/ directly (e.g. `import tcn_forecast`).
testpaths = tests
pythonpath = src
addopts = -q
filterwarnings =
    # torch.nn.utils.weight_norm is deprecated but still used (faithful to upstream TCN).
    ignore::FutureWarning
